import { MemoryRouter } from 'react-router-dom'
import { Meta, Story, Canvas, Preview, Props } from '@storybook/addon-docs/blocks'
import { Icon } from '.'

<Meta title='Components|Icon' component={Icon} />

# Icon
An icon collection covering most use cases;

## Base
The following icons are available:

<Story name='Base'>
  <ul className="icn-list">
    <li className="icn-list__item"><Icon name={'Bin'} /><span className="icn-list__caption">Bin</span></li>
  </ul> 
</Story>

## Sizes
Medium is the default size so the prop can be omitted.

<Preview>
  <Story name='Sizes'>
    <div>
      <Icon name={'User'} size={'XSmall'} />
      <Icon name={'User'} size={'Small'} />
      <Icon name={'User'} />
      <Icon name={'User'} size={'Large'} />
    </div>
  </Story>
</Preview>

## Colours
Dark is the default colour so the prop can be omitted.

<Preview>
  <Story name='Colours'>
    <div>
      <Icon name={'User'} />
      <Icon name={'User'} colour={'Light'} />
      <Icon name={'User'} colour={'Success'} />
      <Icon name={'User'} colour={'Warning'} />
      <Icon name={'User'} colour={'Error'} />
    </div>
  </Story>
</Preview>

## Prop Types

<Props of={Icon} />