import { MemoryRouter } from 'react-router-dom'
import { Meta, Story, Canvas, Preview, Props } from '@storybook/addon-docs/blocks'
import { Button } from '.'

<Meta title='Components|Button' component={Button} />

# Button
A simple button to allow users to carry out an action.

## Primary
For use as a main call to action.

<Preview>
  <Story name='Primary'>
    <Button type={'Primary'}>Primary Button</Button>
  </Story>
</Preview>

## Secondary
Secondary buttons should be used sparingly, in instances where a primary button would have too much emphasis.

<Preview>
  <Story name='Secondary'>
    <Button type={'Secondary'}>Secondary Button</Button>
  </Story>
</Preview>

## Tertiary
Tertiary buttons should be used in conjunction with primary buttons where there are two actions.

<Preview>
  <Story name='Tertiary'>
    <Button type={'Tertiary'}>Tertiary Button</Button>
  </Story>
</Preview>

## Small
Small buttons should be used in conjunction with primary buttons where there are two actions.

<Preview>
  <Story name='Small'>
    <div>
      <Button type={'Primary'} size={'Small'}>Small Button</Button>
      <Button type={'Secondary'} size={'Small'}>Small Button</Button>
    </div>
  </Story>
</Preview>

## Icon
Icon buttons.

<Preview>
  <Story name='PrimaryIcon'>
    <div>
      <Button type={'Primary'} icon={{ name: 'chevron-left', position: 'Left' }} >Primary Icon Button</Button>
      <Button type={'Primary'} icon={{ name: 'chevron-right', position: 'Right' }} >Primary Icon Button</Button>
    </div>
  </Story>
</Preview>

## Action
Action buttons should be used to indicate to the user that they can perform a data related action such as add, edit, delete etc, or can be used in places where there is limited space.

<Preview>
  <Story name='Action'>
    <Button type={'Action'} icon={{ name: 'plus' }}>Action Button</Button>
  </Story>
</Preview>

## Prop Types

<Props of={Button} />